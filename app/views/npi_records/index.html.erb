<p id="notice"><%= notice %></p>

<h1>Npi Records</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @npi_records.each do |npi_record| %>
      <tr>
        <td><%= link_to 'Show', npi_record %></td>
        <td><%= link_to 'Edit', edit_npi_record_path(npi_record) %></td>
        <td><%= link_to 'Destroy', npi_record, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Npi Record', new_npi_record_path %>
